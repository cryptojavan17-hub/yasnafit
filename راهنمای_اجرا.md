# راهنمای اجرا — Yasnafit

این پروژه یک پلتفرم مربیگری فیتنس **Local-First** با Node.js و SQLite است. نیازی به دیتابیس خارجی ندارد.

## پیش‌نیازها

- **Node.js نسخه 22.5 یا بالاتر** (حتما چک کنید: `node -v`)
- **npm** (همراه Node نصب می‌شود)
- سیستم‌عامل: Windows / Linux / macOS

> اگر Node قدیمی دارید: از https://nodejs.org آخرین LTS را نصب کنید.

## نصب

```bash
# 1. کلون
git clone https://github.com/cryptojavan17-hub/yasnafit.git
cd yasnafit

# 2. نصب وابستگی‌ها (در این نسخه وابستگی خارجی ندارد ولی برای اطمینان)
npm install
# اگر package-lock ندارید، خودکار نصب می‌شود
```

## اجرا

### روش ساده (همه سیستم‌ها)

```bash
npm start
# یا
node server.js
```

سرور روی **http://localhost:3020** بالا می‌آید:

```
Yasnafit is running at http://localhost:3020
```

### ویندوز — اجرای با دوبار کلیک

فایل `YASNAFIT-LAUNCHER.bat` را دوبار کلیک کنید. خودش پورت 3020 را باز می‌کند.

### پورت سفارشی

```bash
PORT=4000 npm start
# یا
PORT=4000 node server.js
```

### توکن مربی (اختیاری — برای دیپلوی روی سرور)

به صورت لوکال نیازی نیست. اگر روی سرور عمومی دیپلوی می‌کنید:

```bash
YASNAFIT_COACH_TOKEN=یک_توکن_امن_43_کاراکتری PORT=3020 npm start
```

در حالت لوکال، توکن به صورت خودکار در `data/coach-access-token` ساخته و ذخیره می‌شود.

## مسیرهای اصلی برنامه

| مسیر | توضیح |
|---|---|
| `/` | صفحه اصلی |
| `/coach` | پنل مربی (مدیریت شاگردان) |
| `/student/login` | ورود شاگرد (با موبایل + رمز شخصی) |
| `/student/register` | ثبت‌نام شاگرد جدید |
| `/student/onboarding` | فرم ارزیابی 10 مرحله‌ای |
| `/student/dashboard` | داشبورد شاگرد |
| `/join/:token` | لینک دعوت یک‌بارمصرف (قدیمی) |

## اولین استفاده

1. سرور را اجرا کنید: `npm start`
2. به `http://localhost:3020/coach` بروید — پنل مربی باز می‌شود.
3. یک شاگرد بسازید یا از `http://localhost:3020/student/register` ثبت‌نام کنید:
   - نام کامل
   - موبایل: `09123456789` یا `9123456789` (سیستم دیگر 09 را خودکار اضافه نمی‌کند — دقیقا همان چیزی که وارد می‌کنید ذخیره می‌شود)
   - تاریخ تولد: `1370/01/01` (تقویم جلالی داخلی دارد)
   - استان/شهر/آدرس
   - رمز عبور: حداقل 8 کاراکتر
4. بعد از ثبت‌نام به `/student/onboarding` هدایت می‌شوید — فرم ارزیابی را پر کنید.
   - وزن/قد را می‌توانید فارسی وارد کنید: `۷۰` یا `70 کیلو` هر دو قبول است.
5. با همان موبایل و رمز از `/student/login` وارد شوید.

## دیتابیس

- فایل: `data/yasnafit.db` (SQLite)
- به صورت خودکار با اجرای سرور، مایگریشن‌ها اجرا می‌شوند.
- برای ریست کامل: سرور را ببندید و `data/yasnafit.db` را حذف کنید، دوباره `npm start` بزنید.

## تست‌ها

```bash
npm test
# یا تکی:
npm run test:registration
npm run test:auth
npm run test:assessment
npm run test:students
```

## ساختار پوشه‌ها

```
public/          # فرانت‌اند (student-app.js, assessment-wizard.js, students.js, ...)
src/             # بک‌اند سرویس‌ها (student-auth-service.js, assessment-service.js, ...)
data/            # دیتابیس SQLite و توکن‌ها (auto-created)
tests/           # تست‌های رگرسیون
server.js        # سرور اصلی
```

## مشکلات رایج

**1. صفحه لاگین روی "در حال بارگذاری..." گیر می‌کند**
- کنسول مرورگر (F12) را باز کنید. اگر خطای `Identifier has already been declared` دیدید، مطمئن شوید آخرین کد را پول کرده‌اید (`git pull`).

**2. خطای `وزن نامعتبر است` / `weight نامعتبر است` بعد از مرحله هدف**
- نسخه جدید این را با پشتیبانی از ارقام فارسی و متن اضافی (`۷۰`، `70kg`) حل کرده. اگر هنوز می‌بینید، کش مرورگر را پاک کنید (Ctrl+Shift+R).

**3. شماره موبایل 09 اضافه می‌کند**
- در نسخه جدید `completeMobile` دیگر 09 اضافه نمی‌کند. مقدار دقیق ورودی ذخیره می‌شود. اگر هنوز اضافه می‌کند، مطمئن شوید `public/*.js` آخرین نسخه است.

**4. پورت 3020 اشغال است**
```bash
# لینوکس/مک
lsof -ti:3020 | xargs kill -9
# ویندوز
netstat -ano | findstr :3020
taskkill /PID <PID> /F
```

## نکات برای توسعه

- فرانت‌اند بدون بیلد است — فایل‌های `public/*.js` مستقیم سرو می‌شوند.
- بعد از تغییر `public/` نیازی به ری‌استارت سرور نیست، فقط رفرش مرورگر.
- بعد از تغییر `src/` یا `server.js` سرور را ری‌استارت کنید.

## دیپلوی سریع روی سرور

```bash
git clone <repo>
cd yasnafit
PORT=3020 YASNAFIT_COACH_TOKEN=$(openssl rand -base64 32 | tr -d '/+=' | cut -c1-43) node server.js
```

---

**نسخه فعلی:** 0.9.0 | **پورت پیش‌فرض:** 3020 | **Node مورد نیاز:** >=22.5
