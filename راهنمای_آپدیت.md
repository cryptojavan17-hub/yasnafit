# راهنمای آپدیت — Yasnafit

این راهنما برای کسانی است که قبلا Yasnafit را نصب کرده‌اند و می‌خواهند به آخرین نسخه (طرح لاگین لوکس + فیکس وزن و موبایل) آپدیت کنند.

## نسخه فعلی شما چیست؟

```bash
cd yasnafit
git log --oneline -3
cat package.json | grep version
```

آخرین نسخه باید این کامیت‌ها را داشته باشد:
- `feat(auth): implement luxury dark fitness login UI exactly like reference` (47deb0e / d3b20c4)
- `fix(mobile,assessment): remove auto 09 add everywhere...` (928477d)
- `fix(auth): make login title smaller & remove 09- prefix...` (079cf4e)

## آپدیت سریع (روش پیشنهادی)

```bash
cd yasnafit

# 1. ذخیره تغییرات محلی اگر دارید
git stash

# 2. دریافت آخرین کد
git fetch origin

# اگر روی برنچ arena/01a053d3-yasnafit هستید:
git checkout arena/01a053d3-yasnafit
git reset --hard origin/arena/01a053d3-yasnafit

# اگر روی arena/01a03a02-yasnafit هستید:
# git checkout arena/01a03a02-yasnafit
# git reset --hard origin/arena/01a03a02-yasnafit

# 3. نصب وابستگی (معمولا نیازی نیست، ولی برای اطمینان)
npm install

# 4. حذف دیتابیس تستی اگر قبلا با باگ 09 تست کرده بودید (اختیاری)
# rm data/yasnafit.db

# 5. اجرای مجدد
npm start
# یا
PORT=3020 node server.js
```

بعد از اجرا، مایگریشن‌ها خودکار اجرا می‌شوند:
```
[Migrations] Applying 027_... ✅
Yasnafit is running at http://localhost:3020
```

## پاک کردن کش مرورگر (مهم!)

چون `public/student-app.js` و `public/luxury-login.css` عوض شده، حتما:

- **Chrome/Edge:** `Ctrl + Shift + R` (ویندوز) یا `Cmd + Shift + R` (مک)
- یا `Ctrl + F5`
- یا DevTools (F12) → راست‌کلیک روی رفرش → `Empty Cache and Hard Reload`

در غیر این صورت طرح قدیمی لاگین را می‌بینید.

## چه چیزی در این آپدیت تغییر کرد؟

### 1. طرح لاگین لوکس (جدید)
- فایل‌های جدید:
  - `public/luxury-login.css` (طرح جدید)
  - `public/assets/hero-login.jpg` (عکس قهرمان - دقیقا همان عکس مرجع، بدون تغییر)
- فایل‌های تغییر یافته:
  - `public/student-app.js` → تابع `loginForm()` کاملا بازطراحی شد ولی ID ها (`loginMobile`, `loginPassword`, `btnLoginSubmit`...) حفظ شد
  - `public/student.html` → لینک `luxury-login.css` اضافه شد

### 2. فیکس موبایل 09
- قبلا اگر `912...` وارد می‌کردید، سیستم خودکار `0912...` می‌ساخت
- الان `completeMobile` فقط `asciiDigits` برمی‌گرداند، هیچ `09` اضافه نمی‌شود
- فایل‌ها: `public/assessment-wizard.js`, `public/student-app.js`, `public/students.js`, `src/student-auth-service.js`

### 3. فیکس وزن نامعتبر است (مرحله 2 ارزیابی)
- علت: `Number('۷۰')` → `NaN`
- فیکس: `parseLocalizedNumber` که `۷۰`، `70 کیلو`، `70kg` را به `70` تبدیل می‌کند
- فایل‌ها: `src/student-service.js`, `src/assessment-service.js`, `public/assessment-wizard.js`, `public/student-app.js`, `public/student-portal.js`

## آپدیت روی سرور (Production)

اگر روی سرور با `YASNAFIT_COACH_TOKEN` اجرا می‌کنید:

```bash
cd yasnafit
git fetch origin
git checkout arena/01a053d3-yasnafit
git reset --hard origin/arena/01a053d3-yasnafit
pm2 stop yasnafit || pkill -f "node server.js"
PORT=3020 YASNAFIT_COACH_TOKEN=توکن_شما node server.js &
# یا با pm2:
# pm2 restart yasnafit --update-env
```

## آپدیت ویندوز (BAT Launcher)

1. `YASNAFIT-LAUNCHER.bat` را ببندید
2. پوشه `yasnafit` → راست‌کلیک → `Git Bash Here` (یا CMD)
3. دستورات بالا (`git fetch` و `git reset`) را بزنید
4. دوباره `YASNAFIT-LAUNCHER.bat` را دوبار کلیک کنید

## اگر بعد از آپدیت مشکل داشتید

### لاگین روی "در حال بارگذاری..." گیر کرده
```bash
# چک کنید فایل سالم است
node --check public/student-app.js
# اگر ارور داد:
git checkout origin/arena/01a053d3-yasnafit -- public/student-app.js
```

### عکس لاگین لود نمی‌شود (404)
```bash
# چون assets/ در .gitignore بود، باید force-add شده باشد
ls -lh public/assets/hero-login.jpg
# اگر نبود:
git lfs? نه، فقط:
git fetch origin
git checkout origin/arena/01a053d3-yasnafit -- public/assets/hero-login.jpg
# یا دستی کپی کنید
```

### وزن هنوز خطای `weight نامعتبر است` می‌دهد
```bash
# مطمئن شوید آخرین بک‌اند را دارید
grep -n "parseLocalizedNumber" src/student-service.js
# باید 4 نتیجه بدهد
# اگر نداد:
git reset --hard origin/arena/01a053d3-yasnafit
npm start
```

### پورت 3020 اشغال است
```bash
# لینوکس/مک
lsof -ti:3020 | xargs kill -9
# ویندوز CMD
netstat -ano | findstr :3020
taskkill /PID <PID> /F
```

## برگشت به نسخه قبلی (Rollback)

اگر خواستید به نسخه قبل از طرح لوکس برگردید:

```bash
git log --oneline
# مثلا به 99674ce برگردید (قبل از لوکس)
git reset --hard 99674ce
npm start
```

## خلاصه دستورات یک‌خطی

**آپدیت کامل + اجرا:**
```bash
cd yasnafit && git fetch origin && git checkout arena/01a053d3-yasnafit && git reset --hard origin/arena/01a053d3-yasnafit && npm start
```

**آپدیت + پاک کردن کش دیتابیس تستی:**
```bash
cd yasnafit && git fetch origin && git reset --hard origin/arena/01a053d3-yasnafit && rm -f data/yasnafit.db && PORT=3020 node server.js
```

---

**آخرین کامیت‌ها:**
- `47deb0e` / `d3b20c4` → طرح لاگین لوکس دقیقا مثل مرجع
- `928477d` / `7c951e6` → فیکس موبایل و وزن فارسی
- `99674ce` / `c7a9336` → راهنمای اجرا
