# YasnaFit — Known Issues (مشکلات شناخته‌شده)

> وضعیت‌ها: OPEN / INVESTIGATING / FIXED / WONTFIX / BLOCKED
> موارد FIXED بلافاصله حذف نمی‌شوند — تاریخچه کوتاه نگه داشته می‌شود تا ایجنت‌های آینده بدانند چه گذشته است.

---

## KI-001 | تست e2e بدون سرورِ در حال اجرا fail می‌شود
- **SEVERITY:** Low • **STATUS:** OPEN
- **DESCRIPTION:** `tests/e2e-workflow.js` به `http://127.0.0.1:3020` وصل می‌شود؛ اگر `npm start` از قبل اجرا نشده باشد با `ECONNREFUSED` می‌شکند. بقیه ۸ سوئیت مستقل‌اند.
- **REPRODUCE:** سرور خاموش → `npm run test:e2e` → TypeError: fetch failed.
- **EXPECTED:** یا سرور تستی spawn شود یا پیام راهنما چاپ شود. **ACTUAL:** استک خام fetch failed.
- **POSSIBLE CAUSE:** طراحی تست فرض کرده سرور dev بالا است.
- **NEXT ACTION:** TODO T-011 (spawn خودکار یا چک اولیه).

## KI-002 | شکاف ۵ سیستم وب ↔ ۱۲ سیستم فلوی موبایل
- **SEVERITY:** High (یکپارچه‌سازی) • **STATUS:** **FIXED** (2026-08-24 — BR-14؛ رفع کامل: کاتالوگ ۱۲گانه با idهای تاریخی ۱–۵ حفظ‌شده + سقف حرکت واقعی؛ جزئیات: CHANGELOG Task 5. باقی‌مانده کوچک → T-14 کاتالوگ DB هنگام سینک)
- **DESCRIPTION:** Program Builder وب ۵ سیستم دارد (عادی/سوپرست/تری‌ست/جاینت‌ست/دراپ‌ست)؛ مشخصات فلوی موبایل «افزودن نمونه برنامه تمرینی» ۱۲ سیستم الزام می‌داند (رست پاز، پس خستگی، FST7، ۲۱، تکرار نیمه، ۲۰-۱۰-۵، جايت ست، ماموت ست اضافه‌اند). کاتالوگ مرجع در DB هم وجود ندارد.
- **REPRODUCE:** مقایسه `public/program-builder.js` (systemTypes) با مشخصات موبایل.
- **EXPECTED:** لیست کانونی واحد از کاتالوگ DB. **ACTUAL:** دو منبع جدا (فرانت‌اند vs مشخصات موبایل).
- **POSSIBLE CAUSE:** وب قبل از مستندشدن فلوی موبایل ساخته شده.
- **NEXT ACTION:** TODO T-01. طبق قاعده، هیچ‌طرف بی‌اجازه تغییر داده نشد.

## KI-003 | پخش ویدیو در UI پیاده نشده
- **SEVERITY:** Medium • **STATUS:** OPEN
- **DESCRIPTION:** ستون/فیلد video_path هست و `/files/exercise/videos/*.mp4` سرو می‌شود، ولی هیچ `<video>` یا پلیر در هیچ صفحه‌ای نیست؛ در مودال ویرایش حرکت فقط input متنی مسیر است.
- **REPRODUCE:** در بانک حرکات/Program Builder دنبال پخش ویدیو بگرد.
- **EXPECTED:** پیش‌نمایش ویدیوی حرکت هنگام انتخاب. **ACTUAL:** فقط تصویر ثابت.
- **NEXT ACTION:** TODO T-02.

## KI-004 | `node:sqlite` هنوز experimental است
- **SEVERITY:** Low (فعلاً) • **STATUS:** OPEN (پایش)
- **DESCRIPTION:** Node در هر اجرا ExperimentalWarning می‌دهد؛ API ممکن است در نسخه‌های آینده Node تغییر کند.
- **NEXT ACTION:** پایش changelog Node قبل از ارتقای runtime؛ قفل کردن نسخه Node در محیط مربی.

## KI-005 | کلون تازه بدون ۱٬۸۸۸ عکس حرکات است
- **SEVERITY:** Medium • **STATUS:** OPEN (by design — TD-12)
- **DESCRIPTION:** عکس‌های ایمپورت‌شده و ویدیوها gitignore هستند؛ نصب تازه باید با `YASNAFIT-LAUNCHER.bat` گزینه ۶ از پوشه محلی مالک (`C:\Users\MAHDI\Desktop\bodybuilding\exercises_organized`) ایمپورت کند.
- **EXPECTED:** fallback «تصویر موجود نیست» نمایش داده می‌شود. **ACTUAL:** همین رفتار درست است؛ فقط نیاز به ایمپورت دستی.
- **NEXT ACTION:** مستندسازی در README کافی است؛ در صورت مالک‌شدن سرور، میزبانی رسانه تصمیم جدید می‌خواهد.

## KI-006 | Rate limiter در حافظه است
- **SEVERITY:** Low • **STATUS:** OPEN (WONTFIX تا تک‌کاربره)
- **DESCRIPTION:** باکت‌های rate limit در Map حافظه‌اند؛ با ری‌استارت سرور ریست می‌شوند و در چند process به اشتراک گذاشته نمی‌شوند.
- **NEXT ACTION:** در صورت استقرار سرور مرکزی → TODO T-013.

## KI-007 | داده تستی e2e در DB محلی انباشته می‌شود
- **SEVERITY:** Low • **STATUS:** OPEN
- **DESCRIPTION:** هر اجرای e2e شاگرد/برنامه/اعلان تستی اضافه می‌کند و با اجراهای متوالی انباشته می‌شود (ممیزی 2026-08-24 آخرِ روز: ۲۵ شاگرد، ۹ برنامه، ۳ جلسه تمرین).
- **NEXT ACTION:** TODO T-005 (DB تست جدا یا پاکسازی).

## KI-008 | `tool/program-helper.py` پوشش تست ندارد
- **SEVERITY:** Low • **STATUS:** OPEN
- **DESCRIPTION:** اسکریپت کمکی نگاشت حرکات→ویدیو خارج از `npm test` است؛ فقط تست دستی شده.
- **NEXT ACTION:** TODO T-012.

---

## KI-010 | اجرای متوالی e2e روی همان پروسه سرور به Rate Limit می‌خورد
- **SEVERITY:** Low (زیرساخت تست) • **STATUS:** OPEN
- **DESCRIPTION:** گارد `student-join` (۳۰ درخواست/۶۰ث از هر IP) مانع اجرای متوالی e2e روی **همان پروسه سرور** می‌شود. خودِ تست e2e در انتها به‌صورت عمدی باکت را تا 429 پر می‌کند (`e2e-workflow.js` بخش rate-limit assertion) و باکت در حافظه سرور باقی می‌ماند. مشاهده واقعی (2026-08-24): اجرای اول ✅؛ اجرای دومِ بلافاصله ❌ 429 در ورود اولین شاگرد؛ یک اجرای دیگر حتی پس از ~۹۰ث فاصله ❌ 429 در تست 409؛ **ری‌استارت سرور (پاک‌شدن حافظه) → ✅ PASS کامل**.
- **REPRODUCE:** `npm start` → `npm run test:e2e` → دوباره `npm run test:e2e` (بدون ری‌استارت سرور).
- **EXPECTED:** اجراهای متوالی مستقل باشند. **ACTUAL:** اجرای دوم به 429 می‌خورد.
- **POSSIBLE CAUSE:** باکت‌های در-حافظه rate limiter بین اجراها به ازای IP مشترک (127.0.0.1) باقی می‌مانند؛ رفتار دقیق پنجره ثابت ۶۰ث در عمل با مشاهده‌ها کاملاً منطبق نبود (`NOT VERIFIED` — نیاز تحلیل دقیق‌تر در T-011).
- **NEXT ACTION:** T-011 (سرور تستی spawn خودکار / exempt کردن env تست از limiter). راهکار مطمئن فعلی: **بین اجراهای e2e سرور را ری‌استارت کن** یا از DB/بکاپ تمیز استفاده کن.

---

## KI-012 | جستجوی مسیر «دسته+کوئری» فقط LIKE نرمال‌شده (بدون رتبه‌بندی)
- **SEVERITY:** Low • **STATUS:** OPEN (کفایت عملی دارد)
- **DESCRIPTION:** جستجوی سراسری (بدون دسته) رتبه‌بندی دقیق JS دارد؛ مسیر دسته‌بندی‌دار همان LIKE قدیمی با عبارت نرمال‌شده است (بدون رتبه‌بندی/امتیاز).
- **NEXT ACTION:** در صورت نیاز، همان امتیازدهی روی نتایج دسته اعمال شود (کوچک).

## KI-011 | عدم وجود تست مرورگر واقعی برای UI (بازطراحی برنامه‌ساز با استاب/گارد استاتیک راستی‌آزمایی شد)
- **SEVERITY:** Low-Medium (شکاف تست) • **STATUS:** OPEN
- **DESCRIPTION:** پروژه هیچ اتوماسیون مرورگری ندارد (no puppeteer/playwright). بازطراحی UI برنامه‌ساز (2026-08-24) با ترکیب گارد استاتیک `test:ui` + e2e سطح API + smoke رندر DOM با استاب Node تأیید شد — تعاملات واقعی کاربر (کلیک چیپ روز، منوهای بازشو، ریسپانسیو) فقط با بازبینی دستی قابل تأییدند.
- **EXPECTED:** چند مسیر UI کلیدی با مرورگر هدست‌شونده تست شوند. **ACTUAL:** پوشش استاتیک/استاب.
- **NEXT ACTION:** در صورت افزودن وابستگی dev (پیشنهاد: Playwright) یک سناریوی smoke مرورگری برای برنامه‌ساز نوشته شود. **بهبود 2026-08-24:** رگرسیون داخلی دائمی `tool/smoke-program-builder.js` اضافه شد (جریان کامل UI با استاب — ۲۸ چک)؛ تست مرورگر واقعی همچنان موجود نیست.

## KI-013 | آیتم «رمز ورود» منوی ⋮ لیست شاگردان مرده بود (باگ گزارش‌شده مالک)
- **SEVERITY:** Medium • **STATUS:** **FIXED** (2026-09-02 — Task 13)
- **DESCRIPTION:** دکمه «🔑 رمز ورود» در منوی ⋮ هر ردیف رندر می‌شد اما هیچ `onclick` نداشت؛ کلیک فقط منو را می‌بست و هیچ پنجره‌ای باز نمی‌شد. تابع `openCredentialsModal` در `public/students.js` تعریف‌شده ولی هرگز صدا نمی‌شد.
- **REPRODUCE:** لیست شاگرد → ⋮ → رمز ورود → بدون هیچ واکنش.
- **RESOLUTION:** نقطه ورود از منوی ⋮ حذف و «مدیریت رمز ورود» به پنجره «ویرایش شاگرد» منتقل شد (نام کاربری + رمز جدید/تکرار + بازنشانی به رمز موقت + باز کردن قفل + کپی اطلاعات ورود، با یک دکمه ذخیره). گارد دائمی اضافه شد: هر `action-menu-item btn-*` که رندر می‌شود باید در همان رندرر بایندینگ داشته باشد (`tests/student-credentials-ui-regression.js`).
- **LESSON:** رندر یک دکمه بدون بایندینگ = قابلیت خاموش. هر نقطه‌ورود UI باید یا تست تعاملی داشته باشد یا گارد استاتیک «دکمه ↔ handler».



## KI-014 | سرویس Railway روی `main` بود ⇒ build با «Railpack could not determine how to build the app» شکست
- **SEVERITY:** High برای deploy • **STATUS:** **MITIGATED** (2026-09-02) — رفع نهایی با تصمیم مالک
- **DESCRIPTION:** لاگ build (۲۰۲۶-۰۹-۰۲ ۲۱:۵۵) درختی را نشان داد که فقط `README.md` و `login-hero.png` داشت. `main` در این مخزن شاخهٔ فقط-دارایی است؛ `package.json`، `server.js` و `railway.json` روی آن نیست، پس builder (Railpack) چیزی برای ساختن پیدا نکرد و `start.sh` را هم نساخت. هیچ کدی از برنامه اجرا نشده بود، بنابراین این خطا نشانهٔ باگ برنامه نیست.
- **MITIGATION:** commit شدن `package-lock.json` (پروژه صفر وابستگی است ⇒ تشخیص Node برای Nixpacks و Railpack قطعی می‌شود)، گارد «قفل باید در git باشد» در `test:deployment`، و `DEPLOYMENT.md` §۹.۷ (جدول «متن لاگ ← علت ← کار»).
- **NEXT ACTION:** مالک یا Service → Settings → Source → Branch را روی `arena/01a0618b-yasnafit` می‌گذارد، یا `PR #2` را merge می‌کند (الان `MERGEABLE`/`CLEAN`) و از `main` deploy می‌کند. بعد از اولین build موفق: Volume (§۹.۱ بند ۲) و چک‌های §۹.۵.

---

## تاریخچه FIXED (نگه‌داشته‌شده)

### KI-009 (FIXED) | ویرایش نخست مستندات شامل «اصطلاح ممنوع پروژه» بود
- **SEVERITY:** Low • **STATUS:** **FIXED** (2026-08-24، همان روز ایجاد)
- **DESCRIPTION:** در ویرایش نخستِ همین مستندات، برای توضیح قاعده واژگانی، «اصطلاح ممنوع پروژه» به‌صورت تحت‌اللفظی نوشته شد (PROJECT-CONTEXT، TECHNICAL-DECISIONS، آرشیو گزارش). گارد `terminology-regression` دقیقاً همین را گرفت و تست fail شد.
- **RESOLUTION (مرحله ۱):** حذف توالی ممنوع از ۳ فایل/۵ مورد؛ تست ✅ PASS.
- **RESOLUTION (مرحله ۲ — ممیزی نهایی 2026-08-24):** طبق قاعده جدید مالک، تکنیک تقسیم-واژه هم ممنوع شد؛ همه ارجاع‌ها به «اصطلاح ممنوع پروژه» تغییر کرد؛ گارد مجدداً ✅ PASS.
- **LESSON:** به هیچ وجه خودِ واژه را (کامل یا تقسیم‌شده) ننویسید — فقط «اصطلاح ممنوع پروژه».

- موارد باز این فایل همگی OPEN یا در حالت پایش‌اند. مشکلاتی که در CHANGELOG محصولی ریشه به‌عنوان «Fixes» ثبت شده‌اند (مثل رفع دکمه Next آنبوردینگ در 0.7.2 و پارس اعداد فارسی/عربی در 0.7.1) همان‌جا قابل پیگیری‌اند.
